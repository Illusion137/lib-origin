
// export async function fetch(input: string, init?: RequestInit): Promise<Response>{
//     // const controller = new AbortController();
//     // const timeout_id = setTimeout(() => controller.abort(), 5000);
//     // const response = await fetch(input, {...init, signal: controller.signal }).catch(e );
// }